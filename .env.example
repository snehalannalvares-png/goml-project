# Local dev only — never commit a populated .env
# In production these come from AWS Secrets Manager

DATABASE_URL=postgresql+asyncpg://mxtz:mxtz@localhost:5432/mxtz
AWS_REGION=ap-south-1
S3_BUCKET_NAME=mxtz-media-dev
JWT_SECRET=local-dev-secret-only-change-in-prod
LOG_LEVEL=DEBUG
ENVIRONMENT=dev
